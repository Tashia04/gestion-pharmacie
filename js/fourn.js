$(function () {
  $("table").DataTable();
  // inserer une nouvelle fournisseur
});


// pour recuperer les fournisser a partir de la base de donnee pour modifier
// document.addEventListener("DOMContentLoaded", () => {
//   const updateModal = document.getElementById("UpdateModal");

//   updateModal.addEventListener("show.bs.modal", (event) => {
//     const button = event.relatedTarget; // Bouton qui a déclenché l'ouverture du modal
//     const id = button.getAttribute("data-id"); // Récupère l'ID
//     const fournisseurId = button.getAttribute("data-fournisseur-id"); // Récupère l'ID du fournisseur

//     // Remplir les champs du formulaire
//     document.getElementById("update-id").value = id;
//     document.getElementById("update-Fournisseur").value = fournisseurId;
//   });
// });
